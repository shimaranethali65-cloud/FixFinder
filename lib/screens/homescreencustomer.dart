import 'package:flutter/material.dart';

class HomeScreenCustomer extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Home Customer')),
      body: Center(
        child: Text('Home Screen Customer'),
      ),
    );
  }
}